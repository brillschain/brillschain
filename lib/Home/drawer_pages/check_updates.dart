import 'package:flutter/material.dart';
// import 'package:supplink/Home/constants.dart';

class Check_Updates extends StatelessWidget {
  static const String routeName = '/Check_Updates';
  const Check_Updates({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        //  appBar: myAppBar,
        //  drawer: navi,
        body: Center(child: Text("This is Check_Updates page")));
    // return Center(
    //   child: Text('check_updates'),
    // );
  }
}
