import 'package:flutter/material.dart';

class My_connections extends StatelessWidget {
  const My_connections({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('my connections'),
    );
  }
}
