import 'package:flutter/material.dart';

class NotificationsConnections extends StatelessWidget {
  const NotificationsConnections({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Notifications'
      ),
    );
  }
}