import 'package:flutter/material.dart';


class TrailHome extends StatelessWidget {
  const TrailHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome {}'),
    );
  }
}